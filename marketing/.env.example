# Backend API — where POST /orgs/ lives
NEXT_PUBLIC_API_URL=https://api.employeemonitor.com

# Dashboard app — where Sign in / auth callback lives
# Supabase email verification links redirect here after clicking the link.
NEXT_PUBLIC_APP_URL=https://app.employeemonitor.com
